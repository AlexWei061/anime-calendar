# 自托管重构验证记录

验证日期：2026-09-08。目标是保留现有功能，交付可绑定个人域名的单机部署版本。未购买服务器、修改 DNS、迁移真实用户数据或部署旧 Sites 站点。

## 实现与保留范围

| 范围 | 本次结果 |
| --- | --- |
| 播出表、我的番剧、统计、全库搜索 | 拆分为独立组件，保留 URL 页名、浏览器历史和跨页状态 |
| 桌面周历、移动日程、凌晨放送日、网络先行 | 保留原排期函数、时间轴与无时刻区域 |
| 详情、逐集已看、追番、焦点恢复 | 保留既有交互，修复多集请求失败时的逐集回滚 |
| 账号、私有头像、密码修改 | 迁到 SQLite 与私有文件；保留密码与会话哈希规则；会话操作共享同步锁 |
| 明暗主题与响应式样式 | CSS 拆成 7 文件，按顺序拼接与重构前逐字节一致 |
| 目录与图集更新 | 保留稳定作品 ID，拒绝歧义；支持从已有图集重建输入并在完成后切换映射 |
| 构建与部署 | Next.js standalone、Node 24 Docker 镜像、Caddy、持久化目录和健康检查 |
| 数据维护 | 一致快照备份、校验恢复、D1/R2 离线导入；恢复与导入均写入新目录并撤销旧会话 |

首页入口从 2,064 行拆为 87 行。现有 1,514 个作品 ID、排期资料、生成目录、封面映射和数据库 schema 均未改写。用户原有 `teach/` 与 `lib/calendar.js` 的 29 份已跟踪文件差异与任务前快照完全一致。

## 已执行验证

- `npm test`：严格类型检查、Next.js 生产构建、全部 **193 项测试通过，0 失败、0 跳过**。
- `npm run lint -- --ignore-pattern .worktrees`：0 错误、0 警告。
- `git diff --check`：通过。
- `npm audit --omit=dev`：当次扫描生产依赖 0 个已知漏洞；这不等于对所有开发依赖或未来漏洞的保证。
- 真实生产 HTTP：注册／登录、Cookie、跨账号隔离、151 部追番原子保存、已看范围迁移、头像私有访问、改密撤销会话、故障注入事务回滚、备份与恢复。
- 启动测试故意使用相对 `DATA_DIR`，确认 `npm start` 的入口先解析绝对路径，数据不会随 standalone 切换工作目录而写入 `.next/`。
- 浏览器：登录、跨季度搜索、详情追番、逐集已看、统计联动与焦点恢复；390×844 下单日议程、浅色主题无横向溢出。原桌面暗色布局可正常显示。
- 最终 Docker 镜像：在本机 Linux/arm64、Node 24.20.0 中构建并启动；原生 SQLite 驱动、健康检查、注册、页面、容器重启后会话保留、容器内备份与恢复全部通过。
- `docker compose config --quiet`、官方 Caddy 镜像配置校验：通过。

## 尚需在实际服务器确认

域名 DNS、公开 HTTPS 证书申请、香港服务器及大陆校园网访问体验、实际服务器架构和负载，必须在部署后实测。本次未执行真实旧站导出或用户数据迁移；提供的导入工具已用隔离测试数据验证。当前架构按单应用实例使用，不提供多服务器同时写入同一 SQLite 文件。

上线与恢复命令见 [部署指南](deployment.md)。

# 教学网站验证记录

验证日期：2026-09-17。对象为当前工作区的独立 `teach/`，对应现行 Next.js / Node.js / SQLite / Docker / Caddy 架构。此记录不代表线上版本或学习者已通过验收。

## 深入样板章：本轮验证

第 2 课扩为 21 节 Node / HTTP 样板，保留全站 15 课与原课程 ID。其它课程仍为项目导读，尚未同步扩为同等深度教材。

- `node --test teach/tests/site.test.mjs`：20/20 通过；包括原有检查、新章加载、58 个源码快照一致性、章节直达、非法节号、同章前后退与重复点击保持正文和互动状态。
- `node --test teach/exercises/node-http/examples.test.mjs`：10/10 通过；真实回环 HTTP，覆盖路径/方法/查询、JSON 校验、4096 bytes 上限、非法更新不改变已有状态、重复写入、实例隔离、端口参数与占用错误。
- `NODE_HTTP_SOLUTION=1 node --test teach/exercises/node-http/assessment.test.mjs`：参考答案 6/6 通过；增加重复 GET 及读取后原集合不变的断言。
- 未完成的 `04-student.mjs` 保留 TODO 和 501；更新后的默认验收实测 6 项均仅因 `501 !== 200` 失败，属于教学预期，不纳入项目全绿测试命令。
- 从课程实际渲染的 19 个代码块中提取 remaining 参考片段，在临时的 02 副本中按说明插入：教材六个请求得到 200/200/400/400/400/405，结果为 9/12，405 带 Allow: GET；13 个补充边界请求符合约定，原 health、目录和 404 保持正确。未修改学生文件或示例文件，临时目录与服务已清理。
- 从页面提取并执行 Buffer 演示：输出 `���` 与 `中`，第一行三个 U+FFFD，证明逐块解码和先拼字节再解码的差别。HTTP 测试不保证操作系统的接收分块，也未断言未使用 note 字段的中文解码结果，正文已注明。
- `npm run lint -- --ignore-pattern .worktrees` 通过；补充修改的章节与验收文件单独 ESLint 通过。
- 本轮重跑 `npm test`：类型检查、standalone 构建及原项目 195/195 测试通过，0 跳过。日志：`/private/tmp/anime-teach-node-http-project-test.log`。构建自动改写的 `next-env.d.ts` 已恢复到本轮开始状态。
- 桌面 1280px 与手机 390×844 实际浏览器检查：新章 21 节、21 个目录项；代码与表格局部滚动，整页无横向溢出；章内跳转与后退保留展开提示和 HTTP 推演步骤；非法节号回本章顶部；真实源码和学生作业快照弹窗正常，关闭后恢复焦点。没有浏览器错误日志。
- 测试结束恢复正常桌面视口，未标记课程完成，预览保留在样板章首页。本轮没有新做 PDF 排版验证。

## 导读版：此前自动与命令验证

- `node --test teach/tests/site.test.mjs`：15/15 通过；覆盖 15 课及路由、全部源码引用行号、51 个快照与当前文件逐字及哈希一致、日期/搜索/分栏模型与真实实现对照、请求边界和 5 个故障案例的反馈、导航及异常进度恢复。
- `TEACH_SOLUTION=1 node --test teach/exercises/repair.test.mjs`：参考答案 6/6；未修改的故障练习实测 3/6，符合刻意设置的失败。
- `python3 teach/exercises/schema.py`：内存库成功读取当前迁移，四张业务表与 avatar_version 字段存在。
- `node teach/exercises/operations.mjs`：真实临时 SQLite、WAL、备份与恢复流程 6/6 PASS，随后 CLEANUP。验证快照后新记录不进入旧快照、密码哈希/追番/单集/头像保留、旧会话撤销、拒绝覆盖、损坏检测和失败清理。
- 教材异步示例、内存事务示例已实际执行；备份 shell 模板用不连接服务器的替身检查，失败退出码正确传递，失败时不打印成功目录提示。
- `npm run lint -- --ignore-pattern .worktrees`：通过。
- `npm test`：严格类型检查、Next standalone 构建、195/195 原项目测试通过，0 跳过。构建有 Node module.register 弃用提示，无失败。
- `git diff --check`：通过。构建对 next-env.d.ts 的自动改写已恢复。

## 导读版：此前真实浏览器检查

本机 HTTP 预览，桌面默认视口与 390×844 手机视口：

- 首页、15 课、实验室、源码地图和维护手册均可打开，实验均挂载成功。
- 手机端遍历课程和工具页，页面宽度均为 390px，无整页横向溢出；表格、代码局部可滚动。
- 小测正确答案反馈、403 请求逐步拒绝及 UI 回滚、故障诊断错选/改正后的针对性反馈正常。
- 完成进度刷新后保留；测试完成标记已撤销。
- 课程搜索可匹配备份相关内容；课程 hash 导航和浏览器后退可恢复页面。
- 源码弹窗显示当前文件与哈希；关闭后焦点回到触发按钮。
- 整本打印按钮成功拼接 15 课和手册，未逐页检查实际打印/PDF 排版。

浏览器工具禁止访问 file:// URL，因此未实测直接双击本地文件；入口的本地脚本/样式路径与无远端资源依赖已通过自动检查。HTTP 预览正常；本地文件打开方式需用户在自己的浏览器使用。

## 范围

本次修改教学文件和根 README 的教材入口说明，未修改番剧应用业务逻辑。`docs/deployment.md` 的已有工作区改动予以保留并作为当前教材资料读取。没有提交、推送、连接服务器、迁移真实账号或发布站点。

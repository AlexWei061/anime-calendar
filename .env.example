# Local development can use these defaults. Set the HTTPS origin before deploying.
APP_ORIGIN=http://localhost:3000
DATA_DIR=./storage

# Docker Compose: replace with a domain you own and point its DNS to the server.
SITE_DOMAIN=calendar.example.com
